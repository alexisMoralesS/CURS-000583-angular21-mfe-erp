import { inject, provideAppInitializer } from '@angular/core';

import { AppConfigService } from './app-config.service';

// export function provideAppConfiguration(configPath: string) {
//   return provideAppInitializer(() => inject(AppConfigService).load(configPath));
// }
